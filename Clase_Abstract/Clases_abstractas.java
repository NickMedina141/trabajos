package Clase_Abstract;
public class Clases_abstractas {
    /*
    clases abstractas: 
    -no se instancian
    -metodo abstractos = no tienen cuerpo(solo se define)
    (el metodo abstracto en el principal no se establece en el principal
    pero en las clases hijas estan obligados a implementarlos)
    */
}
